import Foundation

enum Assets {
    enum Images {
        enum Nodes {
            enum Emission {
                static let emission: String = "Images/Nodes/Emission/Emission"
            }
        }
    }
}
